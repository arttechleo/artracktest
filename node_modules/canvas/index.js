// Shim: replace node-canvas (needs GTK) with @napi-rs/canvas
const napi = require('@napi-rs/canvas');
module.exports = {
  createCanvas: napi.createCanvas,
  Image: napi.Image,
  ImageData: napi.ImageData,
  loadImage: napi.loadImage,
};
